<p>
  Discount ID#<%= @discount.id %><br>
  Percent: <%= @discount.percent %>%<br>
  Threshold: <%= @discount.threshold %><br>
  <% if @discount.pending_invoice_items? %>
    This item cannot be edited, there are pending invoices
  <% else %>
    <%= button_to "Edit", edit_merchant_discount_path(@discount.merchant.id, @discount.id), method: :get %>
  <% end%>
</p>
